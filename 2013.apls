⍝ 1: Seems a Bit Odd To Me
{(2×⍳⍵)-1}
⍝ 2: Making The Grade
{100×(+/⍵≥65)÷≢⍵}
⍝ 3: What Is In a Word
{≢(' '(≠⊆⊢)(⍵,'  '))}
⍝ 4: Keeping Things In Balance
⍝ This is a mess. I need to improve it.
 {((+/1=⍵)=(+/2=⍵))∧⍵[⍋⍵]≡⍵}{(('('=⍵)+(')'=⍵)×2)~0}
